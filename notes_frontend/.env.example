# Frontend environment variables for notes_frontend

# PUBLIC_API_BASE_URL is used by the Notes UI to send HTTP requests to the backend.
# Example when the backend exposes REST endpoints like:
#   GET    /notes
#   GET    /notes/:id
#   POST   /notes
#   PUT    /notes/:id
#   DELETE /notes/:id
#
# IMPORTANT: Must start with http(s):// and include any base path if applicable.
PUBLIC_API_BASE_URL=http://localhost:8000
